[package]
name = "pocket-vault"
version = "0.1.0"
edition = "2024"
description = "Offline secret manager with age encryption"

[dependencies]
clap = { version = "4", features = ["derive"] }
anyhow = "1"
dirs = "5"
